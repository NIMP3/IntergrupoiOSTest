# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'InterGrupoiOSTest' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InterGrupoiOSTest
  pod 'KeychainSwift', '~> 20.0.0'
  pod 'RealmSwift', '=10.25.2'

  target 'InterGrupoiOSTestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'InterGrupoiOSTestUITests' do
    # Pods for testing
  end
end
